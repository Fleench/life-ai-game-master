<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:GameMaster.CoinApp.ViewModels"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="600"
             x:Class="GameMaster.CoinApp.Views.MainView"
             x:DataType="vm:MainViewModel"
             Background="#F5F5F7">
    <Design.DataContext>
        <vm:MainViewModel />
    </Design.DataContext>

    <Border Padding="40" MaxWidth="500" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Spacing="30">
            <!-- Header with Refresh -->
            <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,10">
                <TextBlock Grid.Column="0" Text="GameMaster Economy" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="#1C1C1E"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left" />
                <Button Grid.Column="1"
                        Command="{Binding LoadProfileCommand}"
                        Content="Refresh"
                        Background="#007AFF"
                        Foreground="White"
                        CornerRadius="8"
                        Padding="16,8"
                        FontWeight="Medium"
                        VerticalAlignment="Center"/>
            </Grid>

            <!-- Permission Notice Banner (visible only when awaiting permission) -->
            <Border Background="#FFF3CD" 
                    CornerRadius="12" 
                    Padding="16"
                    IsVisible="{Binding IsAwaitingPermission}">
                <StackPanel Spacing="8">
                    <TextBlock Text="⚠️ Permission Required" 
                               FontWeight="SemiBold" 
                               Foreground="#856404" 
                               FontSize="16"/>
                    <TextBlock Text="{Binding PermissionStatusMessage}" 
                               TextWrapping="Wrap"
                               Foreground="#856404" 
                               FontSize="13"/>
                </StackPanel>
            </Border>

            <!-- Points Card -->
            <Border Background="White" CornerRadius="16" Padding="24" BoxShadow="0 4 16 0 #1A000000">
                <StackPanel Spacing="8">
                    <TextBlock Text="POINTS" FontSize="14" FontWeight="SemiBold" Foreground="#8E8E93" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Points}" FontSize="48" FontWeight="Light" Foreground="#007AFF" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Coins Card -->
            <Border Background="White" CornerRadius="16" Padding="24" BoxShadow="0 4 16 0 #1A000000">
                <StackPanel Spacing="16">
                    <TextBlock Text="COINS" FontSize="14" FontWeight="SemiBold" Foreground="#8E8E93" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Coins}" FontSize="64" FontWeight="Bold" Foreground="#FF9500" HorizontalAlignment="Center"/>
                    
                    <Grid ColumnDefinitions="*,20,*" Margin="0,10,0,0">
                        <Button Grid.Column="0" 
                                Command="{Binding RemoveCoinCommand}" 
                                Content="-" 
                                FontSize="32"
                                FontWeight="Medium"
                                Background="#F2F2F7"
                                Foreground="#FF3B30"
                                HorizontalAlignment="Stretch" 
                                HorizontalContentAlignment="Center"
                                CornerRadius="12"/>
                        
                        <Button Grid.Column="2" 
                                Command="{Binding AddCoinCommand}" 
                                Content="+" 
                                FontSize="32"
                                FontWeight="Medium"
                                Background="#F2F2F7"
                                Foreground="#34C759"
                                HorizontalAlignment="Stretch" 
                                HorizontalContentAlignment="Center"
                                CornerRadius="12"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
